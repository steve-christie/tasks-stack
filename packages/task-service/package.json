{
  "name": "tasks-service",
  "version": "1.0.0",
  "scripts": {
    "tasksvc:build": "echo building $npm_package_name && yarn exec rimraf dist && yarn exec tsc --build",
    "tasksvc:start": "node ./dist/server.js"
  },
  "dependencies": {
    "express": "4.18.2"
  },
  "devDependencies": {
    "@types/express": "4.17.21"
  }
}
